import { Box, Typography } from "@material-ui/core";

const Home = () => {
  return (
    <Box margin={2}>
      <Typography variant="h3">Bits and Bytes</Typography>
    </Box>
  );
}

export default Home;
